---
description: Convene multiple personas — fanout, chained red-team, or roundtable
argument-hint: --personas="a,b,c" --mode=fanout|chain|roundtable --prompt="..."
---

Use the `persona-panel` skill.

$ARGUMENTS

Flags and plain language are both fine. Resolve every persona before spawning
anything, print the roster, mode and sub-agent count for confirmation, then run
the topology and produce the chairman synthesis. Persist the run to the
configured memory path.
